# Hue UI — Design Companion

Human companion for **oi-hue-ui**. Executable rules live in `SKILL.md`.

## Asset map

| Path | Purpose |
|------|---------|
| `styles/references/` | Templates and generation standards |
| `styles/examples/` | Canonical outputs for quality calibration |

## Example set

- `styles/examples/*/design-model.yaml` — token and component source of truth
- `styles/examples/*/landing-page.html` — narrative brand expression
- selected `component-library.html` / `app-screen.html` — deeper UI validation

Use examples as baselines, not copy-paste templates.

## Repository routing

1. `<pkg-dir>/page/oi-hue-ui/SKILL.md` — read Usage, then run the workflow.
2. `DESIGN.md` — this file (optional context).
